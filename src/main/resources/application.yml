spring:
  application:
    name: vending-machine

  r2dbc:
    name: postgres
    url: r2dbc:postgresql://localhost:5432/postgres
    username: root
    password: password

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

    show-sql: true

server:
  port: 8087